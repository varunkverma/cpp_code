Time Complexities:
- O(1)
    front(), back(), size(), begin(), end(), erase(), push_front(), pop_front(), push_back(), pop_back()

- O(n)
    reverse(), unique(), remove()

    note: unique() removes consecutive duplicates.

- O(nlogn)
    sort()